# Local development only: publishes ports on your machine.
# `docker compose up` loads this file automatically; Coolify uses docker-compose.yaml and routes your domain instead.
services:
  geno-ai:
    ports:
      - "${HOST_PORT:-5000}:5000"
